<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".articles.ui.ArticleDetailFragment">
    <ImageView
        android:id="@+id/detail_header_imv"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@id/detail_header_title"
        app:layout_constraintTop_toTopOf="parent"
        android:src="@mipmap/ic_launcher_round"
        android:layout_marginBottom="@dimen/margin_large"
        android:scaleType="fitXY"
        app:layout_constraintHeight_percent="0.4"
        />

    <TextView
        android:id="@+id/detail_header_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/TextSubtitle"
        android:gravity="center"
        android:text="Hello"
        android:layout_margin="@dimen/margin_xsmall"
        app:layout_constraintTop_toBottomOf="@id/detail_header_imv"

        />
</androidx.constraintlayout.widget.ConstraintLayout>